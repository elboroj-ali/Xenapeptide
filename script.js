const products=window.PRODUCTS||[];
const grid=document.getElementById('productGrid');
if(grid){grid.innerHTML=products.map(p=>`<article class="card"><span class="num">${p.id}</span><a href="product.html?id=${p.id}"><img src="${p.image}" alt="${p.name} ${p.strength}" loading="lazy"><div class="card-body"><h3>${p.name}</h3><div class="strength">${p.strength}</div>${p.price!=null?`<div class="price">€${p.price}</div>`:''}<div class="research">View research information →</div></div></a></article>`).join('')}
const detail=document.getElementById('productDetail');
if(detail){const id=Number(new URLSearchParams(location.search).get('id'))||1;const p=products.find(x=>x.id===id)||products[0];detail.innerHTML=`<div><img src="${p.image}" alt="${p.name} ${p.strength}"></div><div><span class="eyebrow">RESEARCH CATALOG ${String(p.id).padStart(2,'0')}</span><h1>${p.name}</h1><h2>${p.strength}</h2>${p.price!=null?`<div class="price">€${p.price}</div>`:''}<div class="research-box"><h2>What is ${p.name}?</h2><p>${p.whatIs}</p></div><div class="research-box"><h2>Significance in scientific research</h2><p>${p.significance}</p></div><div class="research-box"><h2>Brief summary</h2><p>${p.summary}</p></div><div class="research-box"><h2>Research-use notice</h2><p>This catalogue is informational and research-oriented. It is not medical advice, diagnosis, treatment guidance, or a claim that any research material is safe or effective for human use.</p></div></div>`}
