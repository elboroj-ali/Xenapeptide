let P=[],cart=JSON.parse(localStorage.getItem('xenaCart')||'[]');const $=s=>document.querySelector(s);function count(){document.querySelectorAll('#count').forEach(x=>x.textContent=cart.length)}function add(id){cart.push(id);localStorage.setItem('xenaCart',JSON.stringify(cart));count()}function card(p){return `<article class="card"><span>${p.badge}</span><img src="${p.image}" alt="${p.name}"><div><h3>${p.name}</h3><small>${p.amount}</small><strong>€${p.price}.00 <button onclick="add('${p.id}')">🛒</button></strong></div></article>`}function render(){if(!$('#catalog'))return;let q=($('#search')?.value||'').toLowerCase(),a=P.filter(x=>x.name.toLowerCase().includes(q));let s=$('#sort')?.value;if(s==='low')a.sort((x,y)=>x.price-y.price);if(s==='high')a.sort((x,y)=>y.price-x.price);$('#catalog').innerHTML=a.map(card).join('')}fetch('products.json').then(r=>r.json()).then(x=>{P=x.products;if($('#featured'))$('#featured').innerHTML=P.slice(0,6).map(card).join('');render();count()});